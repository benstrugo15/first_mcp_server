version: '3.9'

services:
  mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-server
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    command: python main.py
